---
description: Git 提交规范和操作习惯
---

## Git 操作规则

1. **不要修改暂存区状态**：暂存区应只保留所有代码和配置改动，不包括编译后的文件，禁止使用 `git reset HEAD` 将文件移出暂存区
2. **单独提交指定文件**：需要只提交某几个文件时，使用 `git commit -m "信息" -- <文件路径1> <文件路径2>` 的方式
3. **提交前确认分支**：commit/push 前先确认当前分支是否正确